<div class="post">
  <% posts.forEach(post => { %>
    <div class="content">
      <h1 class="post__heading"><%= post.title %></h1>
      <h4 class="post__body"><%= post.body %></h4>
    </div>
    <h3 class="post__date"><%= post.updatedAt.toDateString()%></h3>
  <% }) %>
</div>